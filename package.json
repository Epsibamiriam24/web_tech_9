{
  "name": "based-resume-screening-auth",
  "version": "1.0.0",
  "description": "Authentication demo for Based Resume Screening Platform (registration + login)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && cd client && npm install && npx vite build && cd ..",
    "build:client": "cd client && npm install && npx vite build",
    "dev:server": "node server.js",
    "dev:client": "cd client && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "mongoose": "^7.5.0"
  }
}
